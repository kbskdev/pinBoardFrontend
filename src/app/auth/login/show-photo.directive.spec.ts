import { ShowPhotoDirective } from './show-photo.directive';

describe('ShowPhotoDirective', () => {
  it('should create an instance', () => {
    //const directive = new ShowPhotoDirective();
    //expect(directive).toBeTruthy();
  });
});
